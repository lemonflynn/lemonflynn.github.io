---
layout: post
title:  "GCN 计算架构分析 - GEM5/MIAOW"
date:   2025-8-28 6:00:16 +0800
categories: GPU
---

本文，首先从 GEM 5 中的 APU 实现来分析 GCN 计算架构的实现方式，便于理解其整体运行方式，最后再基于 MIAOW 来看看，在硬件 RTL 层面如何实现一个 CU。篇幅有限，在这里先省略对图形渲染相关的分析。

## GCN 架构 overview

![image-20260811081543657](/assets/fpga/gcn.png)

GCN 属于 AMD 的第四代 GPU 架构，上一代 TeraScale 还是基于 VLIW 的 SIMD，也就是需要借助编译器来静态编排指令，来实现 ILP，最新的是 RDNA 和 CDNA，具体如下表：

| 架构代际  | 核心架构                       | 代表系列 / 型号                                              | 核心制程              |                        关键新技术特性                        |
| :-------- | :----------------------------- | :----------------------------------------------------------- | :-------------------- | :----------------------------------------------------------: |
| **第1代** | **GCN 1.0** (Southern Islands) | Radeon HD 7700/7800/7900 系列 Radeon HD 8000 系列 (OEM)      | 28nm                  |            首代GCN架构，统一寻址，支持OpenCL 1.2             |
| **第2代** | **GCN 2.0** (Sea Islands)      | Radeon HD 7790, R7/R9 260/260X                               | 28nm                  |                改进的几何引擎，支持OpenGL 4.3                |
| **第3代** | **GCN 3.0** (Volcanic Islands) | Radeon R9 285/290/390, Fury 系列 (Fiji)                      | 28nm                  |          首个支持HBM高带宽显存 (Fiji)，改进色彩压缩          |
| **第4代** | **GCN 4.0** (Polaris)          | **RX 400 系列** (Polaris 10/11) **RX 500 系列** (Polaris 20/21/12) | 14nm FinFET           |   首次采用14nm工艺，大幅提升能效比，支持HDMI 2.0b和DP 1.4    |
| **第5代** | **GCN 5.0** (Vega)             | RX Vega 56/64, Radeon VII Radeon Pro 系列                    | 14nm/7nm              | 引入HBCC高带宽缓存控制器，支持FP16半精度运算，Radeon VII为首款7nm游戏卡 |
| **第1代** | **RDNA 1.0**                   | **RX 5000 系列** (Navi 10/14)                                | 7nm                   | 专为游戏优化，重新设计流水线，每时钟性能提升25%，支持PCIe 4.0 |
| **第2代** | **RDNA 2.0**                   | **RX 6000 系列** (Navi 21/22/23)                             | 7nm                   | **引入硬件光线追踪 (Ray Accelerator)**，加入Infinity Cache，支持Smart Access Memory |
| **第3代** | **RDNA 3.0**                   | **RX 7000 系列** (Navi 31/32/33)                             | 5nm (GCD) + 6nm (MCD) | 采用**Chiplet (小芯片)** 设计，率先支持DisplayPort 2.1，AI加速器性能提升 |

我们来看看 GCN3 的架构图：

![image-20260811083427914](/assets/fpga/gcn1.png)

一个 Compute-unit 内部的架构如下图：

![image-20260811084146827](/assets/fpga/gcn2.png)



## AMD gem5 APU simulator at CL

gem5 可以模拟一个 APU 系统，其整体架构如下图：

![image-20260813134111661](/assets/fpga/gcn3.png)

### how to launch a kernel and wavefront

整体软件和硬件的交互如下图：

![image-20260813134351285](/assets/fpga/gcn4.png)

其中 Command processor 用来处理 CPU 发送来的 PM4 命令包，传统图形渲染或计算的 GPU 任务提交方式如下：

![image-20260813134527663](/assets/fpga/image-20260813134527663.png)

由内核为每个 App 创建一个可供  GPU scheduler 调度的 entity，配合一定的调度算法（round-robin 或 fifo）将该 entity 中的命令提交给 hardware runqueue，然后由上图 GPU 中的 Command Processor 去处理，可以看到，这里的命令出现了多次拷贝，需要从用户态 -> 内核态 -> GPU。最新的工作方式是，GPU 为每个 APP 创建一个 hardware ring-queue，APP 直接往这个 ring 里提交命令，然后由 GPU 去做调度，如下图，其优点如图右边所示：

![image-20260813135102219](/assets/fpga/gcn5.png)

kernel launch 所涉及到的组件如下图：

![image-20260813135544880](/assets/fpga/gcn6.png)

上图中的 HW Queue Schduler 和 HSAPP， 我觉得可以对应到下图（来自 AMD 的 MES 手册）中的 MES engine 和 Compute QueueMgr：

![image-20260813135942768](/assets/fpga/gcn7.png)

HW queue scheduler 的工作流程如下图：

<img src="/assets/fpga/gcn8.png" alt="image-20260813140223532" style="zoom:30%;" /> 

1. driver write queue’s doorbell register, after fill CMD into Software Queue
2. hw queue sched would starts to get commands from this queue via dma read 

3. once read a pkt, process this pkt, if this’s a dispatch pkt, insert  AQL pkt into dispacher’s queue(all hw_queue’s pkt share this queue) with it’s queue_id

4. Dispatcher dispatch kernel to execute
5. when AQL pkt’s last wf finished execute, call hsaPacketProc().finishPkt()

when sw_queue.size > hw_queue.size, call contexSwitchQ every 100ms, contexSwitchQ would map and unmap different HSA software queue at some scheduling logic.

需要注意的是，doorbell register 也分不同类型，对应不同类型的 queue，gem5 中实现的 doorbell 分类有，compute、gfx、SDMAGfx、SDMAPage、ComputeAQL、interrupt handler、RLC 这几种类型，分别对应不同的 queue，SDMA 引擎主要处理数据拷贝，不过 gem5 也是部分实现了 SDMA 命令，对于 Compute 和  ComputeAQL Queue 类型的 queue，使用 PM4 进行命令编码，pkt processor 可以处理如下类型 pkt：

- WRITE_DATA
- MAP_QUEUES
- UNMAP_QUEUES
- RELASE_MEM
- INDIRECT_BUFFER
- SWITCH_BUFFER
- SET_UCONFIG_REG
- WAIT_REG_MEM
- MAP_PROCESS
- RUN_LIST
- QUERY_STATUS
- INVALIDATE_TLBS

可以看到上图中的 pkt 都是和设置相关的，具体的 kernel launch 操作由 HSAPP 完成，其命令来自 HSA software queue 中的命令，由 ComputeAQL queue 的 doorbell 触发 GPU 操作，其可以处理的命令主要是：

- KERNEL_DISPATCH

- BARRIER_AND
- BARRIER_OR

KERNEL_DISPATCH 命令会把相关信息发给 dispatcher queue。

下图中的 GPU Dispatcher 依据 dispatcher queue 中的 AQL pkt 进行 work-group dispatch：

![image-20260813143507593](/assets/fpga/gcn9.png)

现在来分析下 dispatcher 的工作逻辑：

<img src="/assets/fpga/gcn10.png" alt="image-20260814091624334" style="zoom:50%;" />

GPU dispatcher try dispatch all task in hsaQueue every clock

1. foreach CU, call **hasDispResources** to check if the CU can dispatch,  numWfsToSched mark how many wfs will be disp at this SIMD, pay attention to loop sequence. note, shader->n_wf is 10, means have 10 wavefronts per SIMD, numVectorALUs is 4, means 4 SIMD per CU. so `wfList[4][10]` represent all wavefronts in a CU, which type is `std::vector<std::vector <WaveFront*>> wfList`

![image-20260814091806568](/assets/fpga/gcn11.png)

![image-20260814091922483](/assets/fpga/gcn12.png)

2. if task has remaining wfs to dispatch, and this cu can dispatch, call **dispWorkGroup**, wf will be selected. update task dispatch status (like how many wf left to dispatch)

![image-20260814091940583](/assets/fpga/gcn13.png)

startWavefront will clear wavefront's IB, init regs, set PC of the wavefront and mark warefront Running：

![image-20260814092008238](/assets/fpga/gcn14.png)

至此，我们就在 CU 中激活了若干 wavefronts，上面主要介绍了 gem5 中的 amdgpu 模型是如何从 HSA software queue 中解析命令，直到选择、设置和激活 wavefronts，其实为了确保上述流程可以顺利工作，驱动这边，也是做了很多必备工作，我们可以参考一下 mesa 是如何通过 userq 直接下发 PM4 类型的 gfx 命令给 GPU 的，对应到 compute 任务，rocm 和 amdgpu-kfd 驱动的工作流程都类似，值得注意的是，在第 17 步触发了 doorbell，也就是 gem5 中的命令处理入口点：

![image-20260814093047222](/assets/fpga/gcn15.png)

### Inside a CU

下图是 gem5 对 CU 的实现架构图：

![image-20260814130920744](/assets/fpga/gcn16.png)

可以发现，和 GCN 手册中的架构基本一致，其参数配置如下：

	- 每个 CU 由 4 个 SIMD 组成
	- 每个 SIMD 最多同时执行 10 个 wavefront
	- 每个 SIMD 都有自己的 IB、64K Vector Register、16 通路 vector ALU，一个时钟可以处理 16 个 work-item，而一个 wavefront 有 64 个 work-item，所以一个 wavefront 的指令，需要 4 个 clock 才能执行完
	- 4 个 SIMD 共享 Fetch 和 Decode 部分逻辑、TCP、LDS、Scalar-Unit

指令运行流程如下图：

<img src="/assets/fpga/gcn17.png" alt="image-20260814131056108" style="zoom: 50%;" /> 

GCN ISA 有如下几类，gem5 中除了最后一类和图形管线相关的指令没有实现，其余的都有实现：

<img src="/assets/fpga/gcn18.png" alt="image-20260814131337685" style="zoom:50%;" />

gem5 中对 GCN 指令的模拟其实和对 cpu 指令的模拟差不多，比如，VOP2_V_ADD_F32 指令，其实现如下：

<img src="/assets/fpga/gcn19.png" alt="image-20260814131647904" style="zoom: 33%;" />

先来看看，inst fetch 是如何进行的，每个 SIMD 都分别管理 10 个 wavefront context，每个 wavefront context 包含一个 PC 和 instruction buffer

<img src="/assets/fpga/gcn20.png" alt="image-20260814140143225" style="zoom: 33%;" />

在详细的分析 instruction fetch 是如何工作之前，先来看看 gem5 对 amdgpu 的内存系统是如何建模的，

![image-20260814140755010](/assets/fpga/gcn21.png)

由于 GPU kernel 中的指令使用的是虚拟地址，所以 CU 的 sqc_tlb_port 主要用来完成指令的 va->pa 翻译，sqc_port 用来完成访存操作，memory_port 用来完成 vector-data 的访存操作，由于这部分连接关系复杂，我让 gemini 根据 gem5 中的 MI300X 配置生成了连接关系图，当做参考：

![img_v3_0214i_e5a18959-82c7-46f5-b512-a9f1ae2efbcg](/assets/fpga/gcn22.png)

整个取指流程如下图所示：

![image-20260814141647975](/assets/fpga/gcn23.png)

1. **Foreach** wavefront, if wf’s bufferedPCs has inst to decode, decode inst, push into wf’s IB
2. **Foreach** wavefront, if wf is start & not stopped & fetch buf has free space, get nextFetchAddr, insert wf into fetch queue
3. If fetch queue is not empty, use fetch scheduler choose a wavefront, call initiateFetch to fetch instruction
4. Get inst pc Vaddr from wf, get a inst buf from freelist, insert into reserve pc
5. Create a read request pkt to SQCtlb port, tlb will translate va->pa, after finish, tlbport send resp to CU
6. CU send packet to SQC port to fetch instruction at pa
7. after fetch done, move fetch buf from ReservePC to bufferedPCs

接下来就是解码和发射，

![image-20260816104212249](/assets/fpga/gcn24.png)

上图看的不是很清晰，每个 SIMD 都有自己的 scheduler，负责从 10 个 wavefront 中选出可以若干条可以发射指令的 wavefronts

![image-20260816104420176](/assets/fpga/gcn25.png)

例如下图，

![image-20260816104616653](/assets/fpga/gcn26.png)

一个 scheduler 的工作逻辑是从众多 ready 的 wavefronts 中，根据其本周期发射的指令类型，最多选择 5 个不同指令种类的 wavefront，当有多个 wavefront 使用相同的 FU 时，只能根据特定规则，选择一个 wavefront。

解码、发射和执行的整体流程如下图：

<img src="/assets/fpga/gcn27.png" alt="image-20260816105419757" style="zoom:50%;" />

gem5 为每一个 wavefront 都创建了一个 scoreboard，用来追踪指令源操作数的数据依赖是否存在，在 scoreboard check 阶段，一个时钟周期内，会判断所有 wavefronts 是否可以发射，是否存在数据依赖，根据判断结果，将要发射的 wavefront 的动态指令插入到每个 FU 特有的 readyWFs 结构中( 4 个 SIMD、一个 scalar core、一个 global mem pipe、一个 shared mem pipe、一个 scalar mem pipe)，动态指令被分到哪一个 readyWFs 如下图所示：

<img src="/assets/fpga/gcn28.png" alt="image-20260816112321639" style="zoom:50%;" />

在 scheduler 阶段，流程如下：

1. if readyWFs not empty, use scheduler(oldest first?) choose an wf, pop an instr into schList
2. start reg read, mark RFBusy
3. iterate all inst in schList, if regfile is read complete, change state to RFReady
4. pick the first ready inst, send to readyInsts

在 exe 阶段，流程如下：

1. if readyInst has ready instr, call ii->execute()
2. according to func type, mark fu busy at ‘latency’ clock

### 小结

大体上，在这一节就介绍完了 GCN 架构里一个 wavefront 从创建、解码调度、读操作数、执行、到写回的整体脉络，接下来，还有很多实现细节，例如  vector ALU 的实现、 如何处理 mem instruction、分支指令、message 指令、vector 寄存器分配和回收、TLB coalese 的设计实现，cache-coherence 协议设计、Load-Acquire 指令处理的实现，等等，这些代码里都有实现，就太多了，可以自行去分析，这里先战略性 TBD 了。。。

接下来，我们从 RTL 层面来看看，如何实现一个 CU

## MIAOW at RTL level

先来看 MIAOW 中，对一个 CU micro-arch 的概览：

## CU overview

<img src="/assets/fpga/miaow10.png" alt="image-20251215175411508" style="zoom: 50%;" />

可以发现，这个和上一节 gem5 中的图差不多，流水线可以分为以下几个阶段：

![image-20251215175532974](/assets/fpga/miaow11.png)

## fetch & wavepool

<img src="/assets/fpga/miaow2.png" alt="image-20251211082033558" style="zoom:25%;" />

miaow 微架构白皮书中， fetch 和 wavepool 的功能框图是唯一比较详实精确的，上图中的 WF_ID Gen 模块用来计算出 vacant 信号，表明可以从哪些 wf 中取指令（某些 wf 可能被 halt 了，或被 mask out 了），如果 wavepool 中某个 wf 的 instr queue 满了，则会设置  wave_stop_fetch 中对应的 bit，之后 round robin 调度器会选择一个 wf 来从 instr cache 中取出指令 + tag，送入 wavepool  中对应 wf 的 instr queue 中



<img src="/assets/fpga/miaow3.png" alt="image-20251211082246203" style="zoom:40%;" />

wavepool 中则包含一个 wq_pool 和 wavepool_controller，前者负责存储 wf 的指令 + tag，后者则需要处理

1. 接受来自 instr cache 的数据 
2. wave_queue 的流量控制 
3. 根据 halt 和 branch 信号清空对应 wf 的 instr queue
4. 从 scoreboard feeder 选出的 wf 的 wave_queue 中取指令送给 decode 阶段

scoreboard feeder 的逻辑也比较简单，是根据 issue 阶段提供的 valid_wfid 从中降序选出可以 decode 的 wfid

![image-20251211080631524](/assets/fpga/miaow1.png)

## issue: wavefront scheduling and arbiter

issue 模块是整个 CU 中最复杂的模块，要处理指令发射、指令退休、分支处理，其整体框图如下，为简单起见，这里没有画出 finished_wf、barrier_wait、mem_wait 的逻辑，后面有空再补上

![image-20251213103551176](/assets/fpga/miaow4.png)

其中 valid_entry 会根据 decode_wfid、issue_wfid、salu_branch_wfid 来生成有效的 valid_entry_out，表示所有已经解码但还没有发射的 wf，issue flow ctrl 会根据 valid_entry_out 来控制哪些 wf 可以继续取指令，产生 wave_valid_entry 信号也就是上面 scoreboard feeder 的输入，其逻辑如下：

1. when valid goes down (which means inst issued)
2. barrier, branch or wait_cnt inst retired
3. don't ask new inst if last inst is a branch

同时 valid_entry_out 和 fu_table 的输出以及 max_instr_inflight_array、pengding_branches_array、barrier_wait_array 和来自 scoreboard 的 ready_array_data_dependencies 信号进行 & 操作生成 simd_ready_to_issue 信号，共 40 bit，每个 bit 对应 simd 是否可以被发射，当指令有效，没有被 pending，不是 barrier wait，没有达到指令发射上限且不存在寄存器依赖关系时，就可以被置 1，送入 instr arbiter 等待被选择，instr arbiter 中包含，simd_arbiter、simf_arbiter、lsu_arbiter、salu_arbiter 四个 arbiter 分别用来从 40 个 ready 的 wf 中选择出一个 wf，其结果给 alu_issue，再根据不同 fu 的 ready 情况用来做最终的选择，alu_issue 的选择逻辑是，依次轮转 simd、simf、lsu、salu 这个四个 fu，每个 clock 会选择一个 fu，假如当前 clock 选中了要发射属于 simd fu 的指令，则再根据 simd 0-3 的 ready 情况，优先向编号低的 simd  发射指令 ，下个周期会选中 simf fu，然后依次类推。但是这里有个问题，如果当前选中了 simd fu，但是此时所有 simd 都不可用时，alu_issue logic 会怎么办？从代码来分析，貌似是不发射任何指令，这着实有些不合理了。下图中 select 共 9 bit，对应选择 simd 0-3、 simf 0-3、lsu、salu 中的哪个 fu。

![image-20251213112911216](/assets/fpga/miaow5.png)

下图是 scoreboard 模块的功能框图，其主要功能是用来记录寄存器的状态信息，用以解决寄存器依赖问题，其输入共分为以下 5 个：

1. decoded wf 相关的很多信息
2. issue 相关的 wfid
3. vgpr 写回
4. sgrp 写回
5. exec 寄存器相关

![image-20251215144048921](/assets/fpga/miaow26.png)

当新 decode 一个 wf 的指令时，将以下信息

```
lds_base, branch, vcc_wr, scc_wr, exec_wr, m0_wr, instr_pc, opcode, imm_value0/1, source_reg 1/2/3/4, dest_reg 1/2
```

存入 instr_info_table 中，这样当发射一个 wf 指令或 write-back 某个 wf 指令时，就可以检索到对应信息。

busy_gpr_table 中存储了物理寄存器的 busy 情况，在指令发射时置 1，指令退休时清 0。同时，它在指令 decode 时，需要根据所用到的物理寄存器来生成对应的 busy bits 来初始化 gpr_dependency_table 中寄存器依赖信息，假如当前指令使用了 reg0, reg4, reg8, reg 12 作为 src reg, reg 20, reg 24 作为 dst reg，那么最终的 busy bits 是

```c
/*
src1、src4 和 dst1 会连续检测后面 3 个 reg
其余的会连续检测后面 1 个 reg
src  src src src dst dst
1    2  3   4    1   2
1100_00_00_1010_0001_11
src1  |reg0 not busy
      |reg1 not busy
      |reg2 busy
      |reg3 busy
      
src2  |reg4 busy
      |reg5 busy
      
src3  |reg8 busy
      |reg9 busy
      
src4  |reg12 not busy
      |reg13 busy
      |reg14 not busy
      |reg15 busy
      
dst1  |reg20 busy
      |reg21 busy
      |reg22 busy
      |reg23 not busy
      
dst2  |reg24 not busy
      |reg25 not busy
*/
```

gpr_dependecy_table 中标记了 40 个 wavefront，每个 wf 的逻辑寄存器的依赖情况，比如标记了当前 wf 指令使用的第一个 src reg，第二个 src reg，第三个 src reg 是否为 busy，0 表示 busy，会对每个 wf 所使用的寄存器进行 “&=” 操作，只有当 src1、src2、src3、 src4、dst1、dst2 都不 busy 时，才可以继续发射当前 wf 的指令，生成的 ready_array_data_dependencies 共 40 bits，对应 40 个 wf 的寄存器依赖情况，供 issue 模块使用。 ready_array_spr 是用来管理 special register 的，比如 vcc，scc，exec，m0 寄存器等。

gpr_cmp 的作用是当退休一条指令时，根据 dst_reg_addr 和来自 instr_info table 中的对应指令信息，计算出应该清除哪个逻辑寄存器的 busy bit。scoreboard 中共有 5 个这样的模块，分别是 vgpr_alu_cmp, vgpr_lsu_cmp, sgpr_alu_cmp, sgpr_valu_cmp, sgpr_lsu_cmp。

## SIMD

每个 SIMD 包含 16 个 32 位 ALU，每个 wf 有 64 个 work item，如此一来，就需要 4 个时钟周期来运行一个 wf 的一条指令，对应 alu_fsm 中的 ex1、ex2、ex3、ex4 四个状态。SIMD 单元整体设计没有很复杂，如下图所示。

![image-20251215161233023](/assets/fpga/miaow7.png)

## LSU

Lsu 负责从 memory 读取数据到寄存器，以及将寄存器内容写回 memory，其内部由状态机完成，如下

![image-20251215163110457](/assets/fpga/miaow9.png)

## 总结

这个设计中不支持 graphics 功能、虚拟地址，TLB、LDS、缓存一致性协议以及多级 cache 设计（这些在 gem5 - amdgpu 中有实现，是个很不错的学习对象），并且用于 wf 分发的 thread dispather 的设计比较复杂，由于时间所限还没有仔细分析，以及对 SIMT stack 的分析还欠缺，还有很多细节没有仔细看，后面有空再补上吧。整体来看，MIAOW 的实现也是比较完备的，运行 OpenCL 程序应该没问题，有空可以把这个移植到 FPGA 上跑起来玩玩，应该还是很有意思的。

最后来个 CU 的全览吧。

![miaow](/assets/fpga/miaow8.png)

## 参考引用

1. https://chipsandcheese.com/p/gcn-amds-gpu-architecture-modernization
2. THE AMD gem5 APU SIMULATOR: MODELING GPUS USING THE MACHINE ISA
3. MIAOW Whitepaper Hardware Description and Four Research Case Studies